# Dependencies
node_modules/

# Render output
out/
build/

# Raw narration before silence trimming — the trimmed clips in public/vo/ are
# what the project actually plays. Kept locally for comparison, not shipped.
template/public/vo/raw/

# Secrets. The voiceover script reads ELEVENLABS_API_KEY from the environment
# and never writes it anywhere; this is belt and braces.
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
desktop.ini
